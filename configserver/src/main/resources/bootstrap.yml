spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///{FILE_PATH}


server:
  native:
#    search-locations: file:///Users/illary.huaylupo
    search-locations: classpath:/config  #look in the src/main/resources/config folder
  port: 8071